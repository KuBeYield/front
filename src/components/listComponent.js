import React from 'react';
import { Link } from 'react-router-dom';

const ListComponent = ({ title, linkTo }) => {
    return (
        <Link to={linkTo}>
        
        </Link>
    );
};

export default ListComponent;